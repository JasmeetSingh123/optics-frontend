import masterLoadingSlice, { selectMasterLoadingState, toggle, makeTrue, makeFalse } from "./masterLoadingSlice";


export {
  masterLoadingSlice, selectMasterLoadingState, toggle, makeTrue, makeFalse,
};